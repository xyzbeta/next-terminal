linters-settings:
  goimports:
    local-prefixes: next-terminal

linters:
  disable-all: true
  enable:
    - errcheck
    - gofmt
    - goimports
    - govet
    - ineffassign
    - typecheck

issues:
  exclude-files:
    - test_ssh.go
